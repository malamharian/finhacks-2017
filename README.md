# finhacks-2017 (Chrome banking extension for Facebook using Vue.js)

The hack idea was to allow users of this extension to do transactions directly from facebook with banking API(selling, buying, etc.). 
